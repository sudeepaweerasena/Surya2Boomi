# Solar Flare Forecasting - Streamlit Cloud Dependencies

# App Framework & Viz
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0

# Dependencies for the Surya package (handled automatically by pip, but good to be explicit for caching)
# Note: PyTorch will be installed by Surya's dependencies

# Install the local Surya package
./Surya
